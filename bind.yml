services:
  softioc:
    extends:
      file: build.yml
      service: softioc

  zookeeper:
    extends:
      file: build.yml
      service: zookeeper

  kafka:
    extends:
      file: build.yml
      service: kafka

  connect:
    extends:
      file: build.yml
      service: connect
    volumes:
      - ./build/install:/kafka/connect
      - ./scripts:/scripts
